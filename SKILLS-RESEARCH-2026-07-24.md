# Deep Research — что нужно репозиторию скиллов под GARAJ.KG, 24 июля 2026

> **Статус: НЕЗАВЕРШЕНО.** Исследование остановлено по просьбе основателя (экономия лимитов)
> после фазы «Экосистема». Отработали 15 агентов из ~31: профилирование (6/6), свод разрывов (1/1),
> веб-исследование экосистемы (8/8), веттинг кандидатов (2/16). **Не отработали:** веттинг
> оставшихся 14 кандидатов, адверсариальная верификация и синтез.
> **Практический вывод:** разделы 1–6 — рабочий материал. Раздел 7 (кандидаты) — сырые лиды,
> НЕ прошедшие проверку на существование, лицензию и дубли; ставить по ним ничего нельзя.
> Ничего в `.claude/skills` этим раундом не менялось — по решению основателя раунд «только отчёт».

Как продолжить с места остановки (кэш прогонов сохранён, повторно платить за отработавшие фазы не нужно):

```
Workflow({
  scriptPath: "/root/.claude/projects/-home-user-skilss/39f6568c-4d9a-5ee8-8fe3-f1f1b13ffd09/workflows/scripts/garaj-skills-deep-research-2026-07-24-wf_07cd8e6e-f73.js",
  resumeFromRunId: "wf_07cd8e6e-f73"
})
```
Кэш действует в рамках той сессии; в новой сессии фазы 1–3 придётся прогнать заново.

---

## 1. Метод

Многоагентное исследование по методике раунда 11.07.2026, с одним важным отличием: **впервые
профилирование шло по живому коду продукта**, а не по прошлому отчёту. Продуктовый репозиторий
`djaha12/auto-superapp-kg` подключён к сессии и склонирован с полной историей (2205 коммитов).

| Фаза | Агентов | Статус | Что дала |
|---|---|---|---|
| Профиль продукта | 6 | ✅ | стек+дельта, ops/CI, боли из ~60 стратегических .md, рост/деньги, мобилка/сторы, дрейф библиотек |
| Свод разрывов | 1 (high) | ✅ | 24 потребности с оценкой покрытия |
| Экосистема | 8 | ✅ | upstream-дрейф, платформа Anthropic, новые источники, безопасность экосистемы, охота под разрывы ×2, локальный рынок КГ, ремесло скиллов |
| Веттинг кандидатов | 2 из 16 | ⚠️ частично | 2 вердикта «ставить» |
| Адверсариальная верификация | 0 из ~8 | ❌ | — |
| Синтез | 0 из 1 | ❌ | — |

Дополнительно проверено вручную (детерминированно, не агентами): дрейф двух копий библиотеки
(`comm`, `diff -rq`), фронтматтер 9 кастомных скиллов, наличие правки безопасности в `upstash-redis-js`,
темп изменений продукта (`git log`).

---

## 2. Профиль GARAJ.KG на 24.07.2026 — дельта за 12 дней

Прошлый отчёт описывал проект по состоянию на 11.07. **За 12 дней — 581 коммит**, поэтому часть
прошлого профиля устарела.

- **Стек** (подтверждено `web/package.json`): Next.js 16.2.7, React 19.2.4, TS 5, Tailwind 4,
  Vitest 4.1.9, `@sentry/nextjs` 10.61, `@anthropic-ai/sdk` 0.104.1, `@vercel/blob`, `web-push`, `sharp`.
  Мобилка — Capacitor **6.2.x** (`mobile/package.json`), актуальная линия в июле 2026 — 8.4.x.
- **Воркфлоу:** 70 файлов на `main` (+8 на боковых ветках), 5558 строк YAML.
- **Память проекта:** `CLAUDE.md` — 6594 строки / 785 КБ; за 12 дней вырос примерно втрое (275 КБ → 785 КБ),
  142 коммита «Память: …». Суммарный автозагружаемый контекст репозитория — ~1,44 МБ.
- **Что закрылось с 11.07** (важно, т.к. старые доки этого не знают): Upstash Eviction OFF + бэкапы (30.06),
  банк-эквайринг подключён 18.07, iOS-приложение в App Store, supply вырос с «<100» до ~1900 лотов.
- **Что открыто:** индексация в Google = 0 страниц при ~1726 в sitemap; `SENTRY_DSN` не задан (Sentry no-op);
  дедлайн Google Play API 36 — 31.08.2026; незакрытый платёжный инцидент 19.07.

---

## 3. Дрейф двух копий библиотеки (проверено вручную)

Скиллы живут в двух местах, и они разошлись:

| | Библиотека `djaha12/skilss` | Рабочий репозиторий `auto-superapp-kg` |
|---|---|---|
| Скиллов | **82** | **56** |
| Общих | 47 (из них **46 байт-в-байт идентичны**) | |
| Только здесь | **35** | **9** |

- **35 скиллов библиотеки не доехали в рабочий репозиторий** — то есть в проекте их у агента фактически нет:
  весь цикл Matt Pocock (`grill-me`, `grill-with-docs`, `to-tickets`, `implement`, `handoff`,
  `setup-matt-pocock-skills`, `research`, `prototype`, `domain-modeling`), `skill-creator`, `mcp-builder`,
  `find-skills`, `brainstorming`, `requesting-/receiving-code-review`, `finishing-a-development-branch`,
  `analytics`, `launch`, `marketing-plan`, `marketing-ideas`, `customer-research`, `copy-editing`,
  `docx`/`pdf`/`pptx`/`xlsx`, `doc-coauthoring`, `canvas-design`, `theme-factory`, `web-artifacts-builder`,
  `algorithmic-art`, `brand-guidelines`, `internal-comms`, `slack-gif-creator`.
- **9 кастомных GARAJ-скиллов не доехали в библиотеку** — они существуют в одном экземпляре, без резервной
  копии в курируемом репозитории: `actions-as-hands`, `garaj-known-traps`, `kv-data-patterns`,
  `app-store-lifecycle`, `garaj-session-protocol`, `founder-runbook-writer`, `garaj-i18n-parity`,
  `garaj-content-week`, `submit-appstore`. Все 9 — с корректным фронтматтером и плотными триггерами;
  это дистиллят реальной боли проекта, самая ценная часть обеих библиотек.
- Единственное расхождение по содержимому — `upstash-redis-js`: в продовой копии добавлена локальная
  пометка «в этом репо нет `@upstash/redis`, работа идёт через свой `kv.ts`». Правка безопасности
  от 11.07 (удалённая инструкция создавать временную базу с TTL 3 дня) на месте в **обеих** копиях.

---

## 4. 24 разрыва: что должно уметь окружение агента

Отсортировано по важности. `нет` / `частично` — насколько закрыто установленными скиллами.

### Не покрыто вовсе

1. **`нет` — Держать проектную память в объёме, который агент реально дочитывает.**
   `CLAUDE.md` 785 КБ помечен «читается автоматически в каждой сессии»; при этом уже отстаёт —
   12 коммитов и 3 мержа за 24.07 в нём не описаны. Нужна связка: скилл ротации/индексации +
   SessionStart-хук (в продрепо нет `.claude/settings.json`) + решение основателя резать файл на срез и архив.
   Частично рядом: `handoff` (сжимает диалог, не файл памяти).

2. **`нет` — Запускать прод-операции через GitHub Actions как единственные руки.**
   Сети в песочнице нет; 70 воркфлоу на main + 8 на ветках. 18 файлов нельзя задиспатчить
   (только push-триггер на мёртвую ветку) — агент пишет новый вместо существующего. 22.07 креды
   переехали в GitHub Environment `production-secrets`, а карта `actions-as-hands` обновлялась 11.07
   и учит несуществующему ритуалу с `vercel_token`-инпутом: следуя ей, агент роняет сторож
   `web/test/workflow-secret-inputs.test.ts`. **В библиотеке этого скилла нет вообще.**

3. **`нет` — Точечно править файлы-гиганты без чтения целиком.**
   `AdminDashboard.tsx` 239 КБ (18 коммитов за 12 дней), `SellWizard.tsx` 108 КБ,
   `CatalogClient.tsx` 102 КБ (21 коммит), `listings.ts` 100 КБ, `admin.ts` 89 КБ,
   `app/api/listings/import/route.ts` 75 КБ. Это топ горячих файлов — то есть ежедневная работа
   и ежедневный расход бюджета сессии.

4. **`нет` — Знать правовые рамки КР по персональным данным и чужому контенту.**
   Массовый импорт с mashina.kg вместе с телефонами продавцов идёт в проде и масштабируется,
   при этом собственные доки объявляют это запрещённым, а регистрация в Реестре держателей ПД
   просрочена с 31.12.2025 (штрафы до 65k/120k сом). В библиотеке — 0 вхождений про персональные данные и КР.

5. **`нет` — Отличать актуальный документ от протухшего.**
   `ROADMAP.md` (29.06) числит supply болью №1 при «<100 авто» — на проде ~1900, и считает
   CRON_SECRET/Eviction нерешёнными (закрыты 30.06). `MARKETING-SMM-PLAN-2026-07-06.md` утверждает
   «эквайринг не подключён» (подключён 18.07) и «приложений нет в сторах» (1.0.2 в App Store).
   Четыре свежих ресёрча не попали в «Карту документов». При этом `garaj-content-week` прямо велит
   читать эти файлы как основу — то есть агент планирует по мёртвым вводным.

### Покрыто частично (скилл есть, но мимо цели)

6. **Не отправить в App Store билд с неверным состоянием.** В TestFlight лежат билды 439–441 с ветки,
   где `capacitor.config.ts` смотрит на Vercel-preview с `?_vercel_share=…`. Пре-флайт `build-ios.yml`
   проверяет фон и edge-swipe, но не `server.url`. Дисциплина «`SHOW_NATIVE_PAYMENTS=false` перед подачей»
   держится только на памяти (`flags.ts:31`, сейчас `true`) — забытый флаг = реджект 2.3.1/3.1.1.
   Есть: `capacitor-apple-review-preflight` (generic-гайдлайны Apple); кастомные `app-store-lifecycle`
   и `submit-appstore` — только в продрепо и **устарели** (говорят про 1.0.1 и гейт `useIsNative`).

7. **Держать скиллы-шпаргалки синхронными с кодом.** Главное правило `app-store-lifecycle` и
   `garaj-known-traps` №15 инвертировано в коде с 20.07 (`useIsNative` → `useHidePaid`,
   `native-bridge.ts:234`); из 7 проверенных якорей строк **4 указывают не туда**; версия кэша парсера
   в скилле v3, в коде v6. С 12.07 в `CLAUDE.md` добавилось 119 маркеров ⚠️КАПКАН/⚠️ПРАВИЛО,
   в скиллы перенесён один. Ровно то преимущество (−40% времени по замеру 12.07) и тает.

8. **Проверять UI реально, а не регулярками по исходникам.** `components` дал 11 723 изменённых строки
   за 12 дней — больше, чем `app` и `lib` вместе. При этом Vitest в `environment: node`, без jsdom и
   Testing Library, Playwright в CI не запускается, а роль проверок играют 8 «контрактных» тестов,
   читающих TSX/CSS через `readFileSync` и матчащих `setTimeout(…360)`: они зелёные при сломанном UI
   и красные при безобидном рефакторе. `webapp-testing` установлен в обоих репозиториях и не применяется.

9. **Писать денежные пути Бакая безопасно.** Инцидент 19.07 не закрыт, пара `BAKAIST*` для сверки
   с выпиской мертва с 13.07, в pending 6 заказов. Свои правила куплены болью: «GetState слеп —
   только авторизованное уведомление + сверка выпиской», «возврат сорванной активации только через
   `refundCharge()`», «дедуп-метка ПОСЛЕ успеха» (нарушено минимум в 5 местах).
   `bakai.ts` — 17 коммитов за 12 дней, `pay-watch.ts` без тестов.
   Есть: `webhook-handler-patterns` (каркас verify → parse → idempotent — прямое попадание), `differential-review`.

10. **Диагностировать «конфиг основателя молча ломает прод».** Неодобренный `NIKITASENDER` рубил
    КАЖДЫЙ SMS-код (4× HTTP 502), незаданный `CRON_SECRET` убивал все 6 кронов (курс замёрз на 87.5),
    Basic-логин вебхука Бакая разошёлся с Vercel и съел живой платёж. «Sensitive» env в Vercel не читаются
    глазами — сверка только по длинам/отпечаткам, через самописные health-эндпоинты и 10 воркфлоу `set-*`.

11. **Получать машинно-читаемый сигнал о поломке прода.** `post-deploy-check.yml:44-63` создаёт issue
    без поиска дубля и без ответственного: открыты #557 (24.07) и #554 (23.07), оба не разобраны среди
    530 issues. Sentry заведён «спящим»: без `SENTRY_DSN` полностью no-op — три аудита называют его
    №1 по ROI. Установленные `sentry-debug-issue`/`sentry-create-alert` бесполезны, пока DSN не задан.

12. **Понимать модель данных Upstash по домашним идиомам.** Единственная БД — Redis через собственный
    REST-клиент на голом `fetch` (`kv.ts`), пакета `@upstash/redis` нет ни в `package.json`, ни в lock-файле.
    ~40 префиксов ключей размазаны по 66 файлам `lib` и 120 роутам, схема нигде не декларирована,
    идут две незавершённые миграции. Установленные `upstash-*` учат `npm install @upstash/redis` —
    агент, следуя им, добавит зависимость поверх `kv.ts`. Защитная пометка есть **только** в продовой копии
    и потеряется при пересинхронизации.

13. **Не терять работу между ходами сессии.** Класс повторялся минимум 4 раза за 5 дней: ветка
    `crm-restructure-leads-aypban` оказалась пустой (сессия умерла до коммита), клон восстановился из старого
    снапшота, локальный main оказался несвязанной историей, фикс биллинга осиротел при `git checkout -B`.
    92 удалённых ветки, 38 не влиты. Лекарства (`using-git-worktrees`, `finishing-a-development-branch`,
    `verification-before-completion`, `handoff`) есть в библиотеке и **не установлены в продрепо**.

14. **Приоритизировать тесты по цене ошибки.** 30 серверных модулей без единого теста, среди них
    `lifecycle.ts`, `pay-watch.ts`, `account-delete.ts` (требование Apple), `user-profile.ts` (ПД),
    `plate-blur.ts` (приватность), `moderation.ts`. За 12 дней добавлено 30 тест-файлов — механика есть,
    приоритизации по риску нет.

15. **Вести находки аудитов как трекуемые задачи.** Аудит 29.06: 31 находка, 11 починено, 20 «на твоё решение»
    без трекера статуса. Среди тикающих: id объявления 8 hex (~50% коллизий при ~77k лотов, `addListing`
    перезаписывает молча), полный скан всех аренд на каждый заход `/rent-company/[slug]`.
    Ни одна не отмечена закрытой позже. `to-tickets` есть в библиотеке — и не установлен в продрепо.

16. **Безопасно пропускать недоверенный контент через Claude API.** Реальная поверхность инъекции —
    не CI, а прод: контент mashina.kg и пользовательских объявлений уходит в Claude в `import-parse.ts`,
    `ai-assistant.ts`, `ai-identify.ts`, `ai-inspect.ts`, `journal.ts`, `translate.ts`, `plate-blur.ts`.
    `agentic-actions-auditor` брали в библиотеку именно под этот риск, но по неверной модели
    («часть воркфлоу запускает AI-агентов» — таких нет ни одного на 92 ветках).

17. **Держать гигиену 70 воркфлоу непрерывно.** 40 из 70 без `permissions`, 53 без `timeout-minutes`
    (включая `build-ios` на macos-15 с 441 запуском), ни один экшен не запинен по SHA, `concurrency`
    только у `ci.yml`. `fetch-brand-logos.yml` с `contents: write` пушит прямо в main → Vercel деплоит
    в обход `ci.yml` и ревью. `purge-test-listings.yml` делает dry-run и `?run=1` в одном прогоне.

18. **Диагностировать нулевую индексацию без доступа к GSC.** Весь SEO-код трёх волн работает вхолостую:
    на 16.07 подтверждено 0 страниц в индексе при ~1726 в sitemap; после 16.07 SEO-записей в журнале нет.
    Установлены `seo-audit`, `programmatic-seo`, `ai-seo`, `site-architecture`, `schema` — они покрывают
    on-page и техничку, которая уже сделана.

19. **Провести Android через дедлайн Play API 36 (31.08.2026, продление до 01.11).** `set-target-sdk.mjs`
    прибит к `TARGET=35`; Capacitor 6.2.x вне поддержки, актуальная линия 8.4.x, апгрейд ломающий,
    вся нативка живёт в 12 самописных `inject-*.mjs`. `build-android-release` ни разу не собрался
    (2 запуска, оба failure 19.06); фактически гоняют `build-android-aab`, который **генерирует новый
    keystore на каждом прогоне** — таким AAB нельзя вести обновления. `capacitor-app-store` советует
    `targetSdkVersion 34` — устарел.

20. **Решить по кэш-модели Next 16.** При 16.2.7 не используется ни одна новая примитива: `'use cache'` = 0,
    `cacheComponents` = 0, `revalidateTag` = 0, `'use server'` = 0; зато 95 файлов `force-dynamic`,
    163 из 205 tsx клиентские. Скиллы Vercel датированы январём 2026 и оперируют «Next.js 13.5+»,
    а `web/AGENTS.md` предупреждает «This is NOT the Next.js you know». Сама инструкция AGENTS.md
    невыполнима в свежем клоне: `node_modules/next/dist/docs/` нет до `npm ci`.

21. **Добавлять строку интерфейса без падения CI и калек в кыргызском.** Одна строка = правка 4 словарей
    + ручной подъём захардкоженного `EXPECTED_DICT` (сейчас 1549). За 12 дней словари тронуты ~38 раз каждый.
    23.07 PR #553 переписал 475 из ~949 строк `ky.ts` из-за буквальных калек; маркетинговые KY-тексты
    никем не вычитываются. Паритет решается скиллом, качество языка — нет (нужен носитель).

22. **Показывать вариант и гасить его дешевле, чем веткой + CI + деплоем.** 21 откат за 12 дней
    (32 с 01.06): кнопка поиска смержена 22.07 и откачена 23.07 байт-в-байт, подготовка 1.0.3 смержена
    и отменена в тот же вечер (сборка убита на 1.5 минуте, macOS-минуты ×10). Флаги — константы в бандле:
    смена = коммит + CI + деплой (10–15 мин), хотя комментарий обещает «5 минут».

23. **Планировать монетизацию под КГ-классифайд, а не под западный SaaS.** Эквайринг живёт с 18.07,
    но продавать нечего: доп-услуги (автоподнятие 30, закреп 149, TG-пост 99, AI-осмотр 149) заперты
    захардкоженной константой `SHOW_EXTRA_PROMOS=false` прямо в `PromoTab.tsx:53` (не env-флагом),
    B2B-подписки салонов (75–89% выручки у аналогов) не построены, кошельки (Pay24, О!Деньги, MegaPay)
    не подключены. Установленный `pricing` оперирует ARPU/churn/per-seat/Van Westendorp — мимо импульсных
    SKU за 30–60 сом. `marketing-plan` вдобавок ссылается на 5 неустановленных скиллов.

24. **Измерять, что приносит подачи и оплаты.** Пикселей Meta/TikTok/GA4 в коде нет ни одного
    (`grep fbq|ttq|gtag` = 0), хотя домен верифицирован в Meta Business 14.07. Своя аналитика делит источник
    на 7 фиксированных бакетов и не связывает «источник → публикация/оплата»; рекламный бюджет для CAC
    вводится руками. Блокер записан 06.07 и не сдвинулся 18 дней. Установленный `analytics` —
    про GA4/GTM/Mixpanel/Segment, то есть не про наш стек.

---

## 5. Что изменилось в upstream с 11.07 (проверено по первоисточникам)

- **obra/superpowers → v6.2.0** (23.07, HEAD `3dcbd5c`); у нас срез v6.1.1 от 02.07. **Изменились все 10
  установленных скиллов.** Крупнейшее — `subagent-driven-development` (541 изменённая строка, новый
  `re-review-prompt.md`); в `finishing-a-development-branch` убран пункт «Discard this work» из меню
  (теперь только по явному запросу с подтверждением) и создание PR сделано forge-agnostic;
  `testing-anti-patterns.md` → `writing-good-tests.md`, переписан.
- **anthropics/skills** — HEAD `1f630fd` (22.07), новых скиллов нет. PR #1463 переписал `claude-api`
  (28 файлов, 558 строк). PR #1447 (17.07) существенно переработал `docx`/`pptx`/`xlsx`: поддержка `.potx`,
  у `pptx` удалены `editing.md` и `pptxgenjs.md` (свёрнуты в SKILL.md) — **наши копии ссылаются на файлы,
  которых в upstream больше нет**.
- **mattpocock/skills** — HEAD `ed37663` (21.07), релиз остался 1.1.0; 24 коммита, из установленных
  изменились три косметически (`grilling`, `wayfinder`, `to-tickets`).
- **coreyhaines31/marketingskills** — `ad-creative` 2.6.0 → 2.8.0 (в т.ч. «creative review page» —
  самодостаточный HTML для согласования креативов), `video` 2.0.1 → 2.1.0.

---

## 6. Два кандидата, успевших пройти веттинг

| Кандидат | Вердикт | Суть |
|---|---|---|
| **superpowers 6.1.1 → 6.2.0** (10 скиллов) | **ставить** | Порча состояния SDD — не теория: в продрепо 633 коммита за 14 дней, фич-волны идут подряд, а установленный SDD ведёт леджер по плоскому пути и приказывает не передиспатчивать «выполненные» задачи. Дублей не создаёт (замена тех же 10 каталогов). Риски низкие; install-хуков нет — копируются только каталоги `skills/*`. Работа: 20–30 мин, атомарно и **в двух местах** (библиотека + продрепо) |
| **anthropics/skills → `claude-api`** (PR #1463) | **ставить** | Прямое попадание в живой код: `journal.ts:381–497` делает ручной цикл досылок на `stop_reason:"pause_turn"` под лимит Vercel `maxDuration=300`; старая версия скилла подавала tool runner как «Recommended» — то есть провоцировала рефакторинг рабочего кода. Локальная копия байт-в-байт равна родительскому коммиту PR — мержить нечего, 5 минут. Оговорка: PR подталкивает к Managed Agents, для GARAJ это не нужно |

Оба вердикта **не прошли адверсариальную верификацию** (фаза не отработала) — перед установкой перепроверить.

---

## 7. Кандидаты без веттинга — 109 лидов (НЕ рекомендации)

Собраны 8 экосистемными агентами, дедуплицированы по имени. Существование, лицензия, репутация автора,
пересечение с 82 установленными — **не проверены**. Приводятся как план работ для следующего раунда.

- **Обновления установленного (20):** superpowers v6.2.0; `claude-api`; `docx`/`pptx`/`xlsx`;
  `grilling`/`wayfinder`/`to-tickets`; `ad-creative` 2.8.0; `video` 2.1.0; ресинк Sentry- и Capacitor-скиллов;
  перенос `actions-as-hands` в библиотеку; перенос в продрепо `handoff`/`to-tickets`/`research`/
  `requesting-code-review`/`finishing-a-development-branch`; доустановка `cro`/`offers`/`ab-testing`/
  `paywalls`/`product-marketing` из marketingskills.
- **Новые скиллы (14):** `next-cache-components-adoption`, `next-dev-loop`, `react-view-transitions`,
  `playwright-cli` (microsoft/playwright), `sentry-instrument`/`sentry-setup-ai-monitoring`/`sentry-sdk-upgrade`,
  `vercel-log-drains-webhooks` (hookdeck), `gh-fix-ci`, `ru-text`, qa-skills (risk-based-testing,
  coverage-analysis), `zizmorify`, `capacitor-app-upgrades` (capawesome).
- **Плагины (14):** `claude-security`, `security-guidance` v2.0.6, `claude-md-management`, `session-report`,
  `hookify` + `claude-code-setup`, `modern-web-guidance` (Google Chrome), официальный партнёрский `playwright`,
  trailofbits (mutation-testing, property-based-testing, testing-handbook), promptfoo (evals + red-team).
- **MCP-серверы (5):** github (тулсет `actions`), sentry-mcp, vercel, Meta Ads MCP, NVIDIA SkillSpector.
- **Кастомные скиллы под проект (25):** `garaj-release-preflight`, `garaj-journal-rotation`,
  `skills-sync-audit`, `garaj-ui-verification`, `smoke-triage`, `garaj-parallel-merge`, `garaj-risk-map`,
  `audit-to-issues`, `garaj-llm-untrusted-input`, `garaj-workflow-hygiene`, `garaj-legal-kg`,
  `garaj-indexing-triage`, `garaj-android-release-keystore`, `kg-payments-contract`, `kg-classified-monetization`,
  `garaj-attribution`, `otp-channels-kg`, `kg-seo-geo-ru-ky`, `garaj-tailwind4`, `skill-vetting` и др.
- **Практики и настройки (36):** `.claude/settings.json` с deny-правилами и песочницей для обоих репозиториев;
  SessionStart-хук (`npm ci` + `additionalContext`); Stop-хук «есть незакоммиченные изменения»;
  `zizmor` + `pinact` в `lint-workflows.yml`; контракт свежести скиллов (`last_verified` + `volatility`);
  бюджет листинга скиллов (`skillOverrides`, `maxSkillDescriptionChars`, сжатие 10 самых длинных описаний);
  профили-плагины вместо плоской папки; `context: fork` для тяжёлых аудит-скиллов; починка описаний
  (14 без «Use when», 9 короче 120 символов, 4 не в третьем лице).

---

## 8. Что я вижу главным по итогам незавершённого раунда

Оговорка: фаза синтеза не отработала, ниже — моё чтение материала, не машинный вывод.

1. **Дефицит не в количестве скиллов, а в их актуальности и месте.** 82 скилла — уже много; при этом
   в рабочем репозитории нет 35 из них, а 9 самых ценных (кастомные GARAJ) существуют в одном экземпляре.
   Первое дешёвое действие — не новые скиллы, а устранение дрейфа в обе стороны.
2. **Шпаргалки протухают быстрее, чем обновляются.** 4 из 7 якорей строк битые, ключевое правило
   инвертировано в коде 20.07, 119 новых капканов в дневнике против одного перенесённого в скиллы.
   Без контракта свежести (`last_verified` + сторож) выигрыш −40% времени из замера 12.07 испаряется.
3. **Пять разрывов вообще не закрываются готовыми скиллами** (память, Actions-как-руки, файлы-гиганты,
   право КР, протухшие доки) — это кастомная работа под проект.
4. **Часть «потребностей» — не скиллы, а операционка основателя**: `SENTRY_DSN`, регистрация в Реестре ПД,
   GSC, keystore для Android. Скилл здесь ничего не ускорит.
5. **Ближайший жёсткий срок — Google Play API 36 (31.08.2026).** При Capacitor 6.2.x вне поддержки и
   keystore, который генерируется заново на каждом прогоне, это самый дорогой из открытых рисков.

## 9. Следующий заход

1. Дореализовать веттинг 14 кандидатов + адверсариальную верификацию (резюм по `resumeFromRunId` выше).
2. Отдельным решением — устранение дрейфа библиотек (35 → продрепо, 9 → библиотека).
3. Ресинк upstream: superpowers v6.2.0, `claude-api`, офисные скиллы (у нас ссылки на удалённые файлы).
4. Контракт свежести скиллов + починка битых якорей строк в кастомных GARAJ-скиллах.
